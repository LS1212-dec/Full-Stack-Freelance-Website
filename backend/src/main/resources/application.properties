# Use environment variables with local defaults (the : syntax provides a fallback)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/freelancer_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:lsikka@12}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Move your secret to an environment variable for security
jwt.secret=${JWT_SECRET:94a08da1fecbb6e8b46990538c7b50b294a08da1fecbb6e8b46990538c7b50b2}
jwt.expiration=86400000

# Render will provide the PORT automatically
server.port=${PORT:8080}
  